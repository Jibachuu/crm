-- Migration v84: companies.edo_id — идентификатор контрагента в системе
-- ЭДО (Диадок/СБИС/Контур и т.п.). Хранится строкой, формат разный у
-- провайдеров (длинный hash у Диадока, GLN/ИНН-производные у других),
-- поэтому без проверок и UNIQUE — менеджеры заполняют как пришло.
--
-- Идемпотентно.

ALTER TABLE public.companies ADD COLUMN IF NOT EXISTS edo_id TEXT;
